<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:background">@color/background</item>
    </style>

    <style name="MyTheme" parent="AppTheme">
        <item name="colorPrimary">@color/my_theme_primary</item>
        <item name="colorPrimaryDark">@color/my_theme_primary_dark</item>
        <item name="colorAccent">@color/my_theme_accent</item>
        <item name="android:background">@color/my_theme_background</item>
    </style>

    <style name="TransparentLargeText" parent="@android:style/TextAppearance.DeviceDefault">
        <item name="android:textSize">18sp</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="TitleText" parent="@style/TransparentLargeText">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="WhiteTitleText" parent="@style/TitleText">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="WhiteCardText">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/transparent_white</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="WhiteCardLargeText">
        <item name="android:textColor">@color/transparent_white</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>
